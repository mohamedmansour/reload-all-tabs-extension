This Google Chrome extension reloads or refreshes all tabs (or windows if
specified) by using the default quick keyboard shortcut (ctrl+shift+r, which 
can be customized). As well, there exists an addon to this extension to have
a refresh button on the toolbar.

Mohamed Mansour - http://www.mohamedmansour.com
